基数排序的代码是非常直观的。在下面的代码中，我们假设n个d位的元素村凡在数组A中，第1位最低，第d位最高
RADIX-SORT（A, d）
  for i = 1 to d
    use a stable sort to sort array A on digit i

基数排序是否比基于比较的排序算法（如快排）更好呢？
哪一个算法更适合依赖于具体实现和底层硬件的特性（例如，快排通常能更有效地使用硬件的缓存），以及输入数据的特征。
利用计数排序的基数排序不是原址排序，当主存的容量比较宝贵时，更倾向于快排
